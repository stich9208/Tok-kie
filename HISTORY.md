# 🐰 Tok-kie 개발 및 업데이트 히스토리 (History)

이 문서는 **Tok-kie (토키)** 프로젝트의 주요 기술적 의사결정, 아키텍처 개선 및 기능 고도화 내역을 기록한 히스토리 문서입니다.

---

## 📌 [2026-08-24] 주요 고도화 및 최적화 내역

### 1. Supabase 클라우드 연동의 Zero-Config 혁신 (OAuth + PKCE + 딥링크)
* **문제점 (기존 UX 허들)**:
  * 사용자가 직접 Supabase SQL Editor를 열고 복잡한 SQL 스크립트를 복사/붙여넣기해야 했음.
  * API Key와 URL을 일일이 복사해서 입력해야 하는 번거로움과 이탈율 발생.
* **해결 및 고도화**:
  1. **Management OAuth 2.0 도입**: 브라우저 로그인 한 번으로 프로젝트를 선택하면, 백그라운드에서 테이블(`sessions`, `steps`), 뷰, RLS 정책까지 **100% 자동 생성(Auto-Provisioning)**되도록 구현.
  2. **네이티브 딥링크(`tokkie://oauth/callback`) 연동**:
     * Slack, Notion, Zoom처럼 웹 브라우저에서 승인 시 **macOS가 Tok-kie 데스크탑 앱을 자동으로 포커스**하며 연동 완료.
     * 웹 도메인이나 포트 번호에 전혀 구애받지 않는 네이티브 앱 UX 완성.
  3. **PKCE (RFC 7636) 국제 보안 표준 적용**:
     * 실시간 암호화 난수(`code_verifier` & `code_challenge`)를 사용하여 **`Client Secret`을 완전히 제거**.
     * 오픈소스 공개 시에도 비밀번호 유출 위험이 0%이며, 오직 `Client ID` 하나만 있으면 즉시 동작.
  4. **하이브리드 지원 (사내망 Fallback)**:
     * 사내 폐쇄망이나 자체 구축(Self-hosted) Supabase 사용자를 위해 하단 아코디언으로 수동 Key 입력 옵션 유지.

---

### 2. 포트 충돌 방지 및 표준화 (`3000` ➔ `3030`)
* **배경**: `3000` 포트는 React, Next.js, Express 등 개발자들의 로컬 작업 공간과 충돌할 확률이 매우 높음.
* **조치**:
  * Tok-kie의 기본 포트를 **`3030`**으로 전면 변경.
  * Next.js dev/start 스크립트, Electron 윈도우 로더, 쉘 스크립트, 문서 일괄 동기화.

---

### 3. 초경량 저발열 및 배터리 절전 최적화 (CPU/Disk 점유율 0%대 달성)
* **문제점 (발열 원인)**:
  * 수집기 데몬이 1초마다 무조건 SQLite DB를 열어 큐를 조회하는 불필요한 I/O 발생.
  * Antigravity 대화 중 수십 MB에 달하는 `transcript_full.jsonl`을 매 턴마다 처음부터 끝까지 중복 파싱하여 CPU 과열 유발.
  * 대시보드가 10초마다 무거운 `sqlite3` CLI 프로세스를 반복 스폰.
* **해결 및 최적화**:
  1. **Antigravity 파서 경량화**: 거대한 `transcript_full.jsonl`을 제외하고 가벼운 `transcript.jsonl`만 타겟팅.
  2. **1.5초 쿨다운 디바운스 & mtime 캐싱**: 파일 수정이 멈춘 후 1회만 스마트 파싱 + 파일 수정 시간(`mtime`)이 같으면 즉시 스킵.
  3. **DB 큐 폴링 주기 완화**: 1초 ➔ **30초**로 대폭 완화하여 평상시 디스크/CPU 쿼리 제거.
  4. **대시보드 인메모리 캐싱**: 로컬 DB 파일에 변화가 없을 때는 `sqlite3` 프로세스를 띄우지 않고 0ms로 응답.
  5. **화면 비활성화 시 자동 절전**: 브라우저 창이 최소화되거나 탭이 가려질 때(`visibilitychange`) 불필요한 폴링 중단.

---

### 4. 아키텍처 및 패키징 방향성 확정 (Electron + 내장 초경량화)
* **검토 내용 (Tauri vs Electron)**:
  * **Tauri**: 10MB 이하의 초경량이지만, macOS(사파리 WebKit)와 Windows(크로미움)의 브라우저 렌더링 미세 차이 및 구버전 사파리 호환성 리스크 존재.
  * **Electron**: Chromium 단일 엔진으로 크로스 브라우징 안정성이 100% 보장됨.
* **최종 결론**:
  * **Chromium의 완벽한 UI 일관성을 유지하는 Electron 방식을 채택**.
  * 향후 수집기(Collector)의 Python 의존성을 제거하고 **TypeScript/Rust로 Electron 내부에 단일 프로세스로 내장**하여 최종 패키징 완성도를 높이기로 확정.

---

## 🗺️ 향후 로드맵 (Upcoming Milestones)

- [ ] **디자인 검수 및 테마 시스템 구조 고도화**:
  - 다크/라이트 모드 일관성, 차트 및 타임라인 컴포넌트의 시각적 완성도 검수.
- [ ] **수집기 TypeScript/Rust 일렉트론 내장 (Zero-Python)**:
  - 사용자 컴퓨터에 Python 설치 요구사항을 완전히 없애고 `.dmg` 단일 설치 파일 완성.
- [ ] **서비스 소개 랜딩 페이지 제작**:
  - Tok-kie의 핵심 가치(실시간 토큰 비용 영수증, 1초 딥링크 페어링 등)를 알리는 웹 랜딩 페이지 제작.
